export declare function collectAdapterPages({ fetchPage, initialCursor, maxPages, maxItems, signal, }?: any): Promise<Readonly<{
    items: any;
    nextCursor: null;
    sourceVersion: any;
    pageCount: number;
}> | undefined>;
